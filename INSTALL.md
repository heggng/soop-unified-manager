# 설치 및 사용 안내

## 기존 스크립트 업데이트

1. 네이버 웨일에서 Tampermonkey 대시보드를 엽니다.
2. 기존 `SOOP 즐겨찾기·구독 통합 관리` 또는
   `SOOP 즐겨찾기 한눈에 관리`의 편집 버튼을 누릅니다.
3. [v1.5.0 스크립트 원본](https://raw.githubusercontent.com/heggng/soop-unified-manager/main/soop-favorite-manager.user.js)을 엽니다.
4. Tampermonkey가 표시한 설치 화면에서 `업데이트`를 누릅니다.
5. Tampermonkey 목록에서 이름이 `SOOP 즐겨찾기 한눈에 관리`, 버전이
   `1.5.0`인지 확인합니다.
6. SOOP 즐겨찾기 페이지를 새로고침합니다.

설치 화면이 자동으로 열리지 않으면 원본 전체를 복사해 Tampermonkey
편집기에 붙여넣고 `Ctrl+S`로 저장해도 됩니다.

이전에 `SOOP 구독 한눈에 관리` 보조 스크립트를 별도로 설치했다면 더 이상
필요하지 않으므로 Tampermonkey에서 비활성화하거나 삭제할 수 있습니다.

## 관리 화면 열기

- SOOP 즐겨찾기 페이지 오른쪽 상단의 `★ 즐겨찾기 관리` 버튼을 누릅니다.
- SOOP의 기존 `스트리머 관리` 버튼을 눌러도 같은 화면이 열립니다.

## 필터

- `전체`, `LIVE`, `고정`, `알림`
- `그룹 전체` 및 계정에 등록된 각 즐겨찾기 그룹
- 상태 필터와 그룹 필터를 함께 적용할 수 있습니다.

## 카드 표시와 설정

- 알림 켜기·끄기
- 즐겨찾기 해제
- 즐겨찾기 그룹 설정
- 상단 고정·해제

## 동작 방식

스크립트는 SOOP 페이지의 즐겨찾기 목록과 그룹 정보를 보기 좋은 카드와
버튼으로 정리하고 기존 설정 메뉴를 호출합니다. 그룹을 확인하는 동안 별도의
로딩 문구를 표시하지 않으며, 응답이 지연되면 요청을 자동으로 중단합니다.
로그인 쿠키와 즐겨찾기 목록은 외부 서버로 전송하지 않습니다.

`즐겨찾기 해제`는 해당 스트리머를 목록에서 즉시 제거하므로 버튼 이름을
확인한 뒤 사용하세요.
